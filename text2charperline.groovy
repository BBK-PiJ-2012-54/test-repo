print "Enter some text ";
String text = System.console().readLine();
int slen = text.length()

for(int i = 0; i < slen; i++)
    println(text.charAt(i));

println "********************"

int j = 0;
while(j < slen)
{
    println(text.charAt(j));
    j++;
}


